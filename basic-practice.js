var deposit

// array
const numbers = [27, 29, 93, 034, 8];
const numberCount = numbers.length;
numbers.push(111);
numbers.pop();
// numbers[2]=545; (this is number chnaging condition for array and push the array)

// check wheather 222 included in the array 
if (numbers.indexOf(222) != -1) {

}
if (numbers.includes(222)) {

}

// loop
// while ,for
// for holo kono ekta element k mane kono ekta element er number of number ber korar jonno kaj kore 

for (const number of numbers) {

}

// function
function fullName(first, seecond) {
    const name = first + ' ' + seecond;
}
const person = fullName('Rahim', 'Ali');

// object holo nijer soriler kotha vaben ba nijer life style er kotha vaben jemon dhorun amar nak kan chok mukh its an object

